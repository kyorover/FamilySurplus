hesokuri-cho/
  ├── App.tsx                          // （極小化）タブナビゲーションと画面切り替えのみを担当
  └── src/
       ├── types.ts                    // 中央スキーマ
       ├── store.ts                    // 状態管理（Zustand）
       │
       ├── functions/                  // 【純粋関数】UIも通信も持たない計算ロジック
       │    └── budgetUtils.ts         // 世間目安の計算、予算の客観的評価ロジック
       │
       ├── components/                 // 【極小UIパーツ】状態(State)を極力持たない純粋な見た目
       │    ├── ui/                    // 汎用UIパーツ
       │    │    ├── CustomButton.tsx  // 共通の青いボタン等
       │    │    └── SectionHeader.tsx // 見出しパーツ
       │    │
       │    ├── dashboard/             // ダッシュボード専用パーツ
       │    │    ├── HesokuriSummaryCard.tsx // 「今月のへそくり」を大きく出すカード
       │    │    └── BudgetProgressBar.tsx   // カテゴリ別の進捗バー（単体）
       │    │
       │    ├── input/                 // 入力画面専用パーツ
       │    │    ├── ExpenseInputDisplay.tsx // 金額の表示エリアとチップ選択UI
       │    │    └── ExpenseInputPad.tsx     // カスタムテンキー本体
       │    │
       │    └── settings/              // 設定画面専用パーツ
       │         ├── BudgetEvaluationCard.tsx// 「堅実な貯蓄モデル」等の評価バッジ表示
       │         └── CategoryBudgetList.tsx  // カテゴリごとの予算入力リスト
       │
       └── screens/                    // 【画面の組み立て】極小パーツを並べ、Storeのデータを渡すだけの接着剤
            ├── DashboardScreen.tsx
            ├── InputScreen.tsx
            └── SettingsScreen.tsx